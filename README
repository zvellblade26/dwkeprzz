Optional leftover packages:

1. system-config-printer
2. epson-inkjet-printer-201401w
3. mpv
4. noto-fonts-emoji
	emoji installation: 
		run sudo pacman -S noto-fonts-emoji
		cp 02-emoji.conf to /etc/fonts/conf.d

5. simple-mtpfs -> github
dep : fuse
		gcc-libs
		libmtp
		autoconf-archive
step:
$ ./autogen.sh
$ mkdir build && cd build
$ ../configure
$ make
$ make install (as root)

6. autocpufreq -> github
